apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cni-validation.fullname" . }}-test-connection"
  labels:
    {{- include "cni-validation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cni-validation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
