apiVersion: v1
kind: Service
metadata:
  name: {{ include "cni-validation.fullname" . }}
  labels:
    {{- include "cni-validation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cni-validation.selectorLabels" . | nindent 4 }}
